# Frontend Env Settings (Vite prefix)
VITE_DEFAULT_PROVIDER=public-gemini

# Backend Serverless Env Settings (Keep secret - do NOT prefix with VITE_)
# Set your Hugging Face Access Token here for local/production serverless function (Qwen/Qwen3-VL-8B-Instruct)
# A free token is required to query the new Inference Providers router.
HF_TOKEN=your_huggingface_token_here
